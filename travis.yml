language: node_js
node_js:
  - lts/*
install:
  - yarn install # npm ci
script:
  - yarn docs:build # npm run build
deploy:
  provider: pages
  skip_cleanup: true
  local_dir: docs/.vuepress/dist
  github_token: bfcbee11f2a1b0b961a2143b024dc38702aefac4
  keep_history: true
  on:
    branch: master
